# science-teacher-skills — 기초 설계 문서 (DESIGN.md)

**버전**: 0.4 (2단계 HWPX 완료 — 2026-08-01)
**상태**: 이 문서가 프로젝트의 canonical 기준이다. 구현 파일(레퍼런스·스펙·스크립트)과
이 문서가 충돌하면 이 문서를 고치거나 구현을 고친다 — 둘이 어긋난 채 두지 않는다.

---

## 1. 목적과 범위

### 1.1 한 문장 정의

Anthropic의 `k12-teacher-skills`(수업 설계·차별화 스킬 플러그인)를 **한국 2022 개정
교육과정 체제로 이식**한 과학 전용 오픈소스 Claude 플러그인. 성취기준 데이터는 한국
학습맵 MCP 2종에서, 문서 산출은 (2단계부터) HWPX로.

### 1.2 해결하는 문제

원본 스킬은 미국 표준 체계(CCSS/NGSS/C3)와 미국 커리큘럼(IM/OpenSciEd), Learning
Commons KG에 결합되어 있어 한국 교사에게는 세 가지가 어긋난다:

1. **표준 체계** — 성취기준 코드·원문·선수관계가 미국 것
2. **pedagogy 전제** — 3차원 학습(SEP/DCI/CCC), 미국 학제(K–12), 미국 커리큘럼 분기
3. **문서 형식** — docx 중심 (한국 학교 행정 맥락은 HWP/HWPX)

### 1.3 범위에 포함

- `ko12-lesson-planning` 스킬 (1차, 완료) · `ko12-lesson-differentiation` 스킬 (2차, 완료)
- 과목: 과학 단일 (초·중등 과학, 고교 통합과학·과학탐구실험·선택 과학)
- 학교급: 초3 – 고3 보통교과 (초1–2는 과학 교과가 없어 특례 처리, ADR-2)
- MCP 번들: 한국 학습맵 2종

### 1.4 범위에서 제외 (명시적 non-goals)

- 과학 외 과목 — 과학 전용으로 범위 확정 (2026-08-01). 초기 설계의 다과목 확장 계획
  (구 ADR-5, BATCH_SPEC.md)은 폐기했다.
- 특성화고 전문교과·특목고 계열 (학습맵 데이터 자체가 없음)
- 검정 교과서 콘텐츠 재현 (저작권 + 출판사 중립 원칙, §5.4)
- 평가(채점·루브릭·생기부) **스킬** — 원본에도 없고, 별도 프로젝트 영역.
  (산출물 품질을 채점하는 벤치마크 루브릭 `evals/`는 교사 대면 스킬이 아니라 QA
  인프라라 이 제외에 해당하지 않는다 — §7 검증 층과 §9 문서 지도 참조)
- 오개념 데이터베이스 구축 — 이 프로젝트는 **소비자**이며 공급자가 아님 (§6.2)

---

## 2. 원본 아키텍처 이해 (변경 전 기준선)

원본의 구조적 사실 — 이식 설계는 전부 여기서 도출된다:

```
plugin/
  .mcp.json                     ← 미국 에듀테크 MCP 9종 번들
  skills/k12-lesson-planning/
    SKILL.md                    ← 과목 중립 오케스트레이터 (Step 0~5)
    references/
      learning-commons-kg.md    ← KG 호출 시퀀스 (Step 2에서만 로드)
      {math,ela,science,social_studies}.md   ← 과목별 pedagogy 전체
      example_lesson.json       ← 산출 JSON 예시
    scripts/                    ← block-document JSON → docx/html 렌더러
```

핵심 설계 속성 3가지 (이식에서 **보존**한다):

- **관심사 분리가 파일 경계와 일치한다.** 표준 조회(kg.md) / 과목 pedagogy(과목.md) /
  오케스트레이션(SKILL.md) / 렌더링(scripts)이 서로 침범하지 않는다. 따라서 치환도
  파일 단위로 가능하다.
- **KG는 선택적 강화다.** 미연결 시에도 스킬이 완전 동작한다 (면책 푸터로 강등).
  이식판에서도 학습맵 MCP는 필수 의존이 아니다.
- **산출 일관성은 스키마가 보장한다.** 문서 4종이 하나의 material-source JSON에서
  렌더되어 드리프트가 불가능하다. 이 스키마는 무수정 유지가 원칙이다 (렌더러 구성은
  ADR-4 이력 — 2026-08-03부터 교사 전달물은 HWPX, HTML은 내부 작업 파일).

---

## 3. 이식 아키텍처 — 치환 3지점

| # | 원본 | 이식 | 방식 | 단계 |
|---|---|---|---|---|
| A | Learning Commons KG | 학습맵 MCP 2종 (초등/중등) | `learning-commons-kg.md` → `curriculum-kr-mcp.md` 파일 치환 | 1 |
| B | 미국 과학 pedagogy 레퍼런스 (science.md) | 2022 개정 체제 과학 레퍼런스 | 치환 규칙 적용해 재작성 | 1 |
| C | docx 렌더링 | HWPX 교사 전달 (OWPML 직접 생성) + HTML 작업 파일 | 어댑터 추가(2단계) 후 docx 산출 제거(3단계, 2026-08-03) | 2–3 |

```
                    ┌─ SKILL.md (오케스트레이터, 최소 diff)
                    │
   Step 2 ──────────┼─ curriculum-kr-mcp.md ── curriculum-kr-secondary (MCP)
                    │                       └─ curriculum-kr-elementary (MCP)
   Step 3 ──────────┼─ science.md
                    │
   Step 5 ──────────┴─ scripts/ ── render_all.sh ── render_documents.py (HTML)
                                              └── render_lesson_hwpx.py (OWPML 직접 생성)
```

---

## 4. 핵심 설계 결정 (ADR)

각 결정은 [결정 / 근거 / 기각한 대안 / 재검토 조건] 형식. 검증 중 뒤집히면 여기를 고친다.

### ADR-1. 목표 프레임: 내용 체계 3범주 (지식·이해 / 과정·기능 / 가치·태도)

- **결정**: NGSS 3차원(SEP/DCI/CCC)의 자리에 2022 개정 내용 체계 3범주를 놓는다.
  수업 목표·관찰 템플릿·설계 노트가 전부 이 프레임으로 진술된다.
- **근거**: (a) 구조 동형 — 셋 다 "기능/실천 × 내용 × 횡단 요소"의 3원 구조라 원본의
  "차원을 합치지 말라" 원칙이 그대로 이전된다. (b) 성취기준 문서 정합 — 학습맵이 반환하는
  성취기준 원문·영역 구조가 내용 체계 기반이다.
- **기각한 대안**: 과학과 교과 역량 프레임. 역량은 총론-각론 연계 서술이라 개별 차시
  목표로 내리면 진술이 공허해지기 쉽고, 성취기준 코드와의 대응이 3범주보다 느슨하다.
- **재검토 조건**: 과학 파일럿에서 3범주 진술이 실제 수업 목표로 부자연스럽다고 판단되면.
- **파일럿 결과(2026-08-01)**: 중·초 수업 각 1회에서 3범주 분리 진술이 모두 성립했다. 지식·이해와
  과정·기능은 자연스러웠고, **가치·태도가 가장 쓰기 어려웠다** — 관찰 가능한 태도로 진술하지 않으면
  공허해지기 쉽다(파일럿에서는 "근거를 들어 지적하는 태도", "끝까지 듣고 본 것을 근거로 말하는 태도"로
  구체화). **현직 교사 판정(2026-08-01): 승인** — 파일럿 3건의 가치·태도 진술을 검토해 실제 수업
  목표로 자연스럽고 관찰 가능하다고 확인. ADR-1 검증 완료, 미결 없음.

### ADR-2. 학년군 분기: 초3–4 / 초5–6 / 중1–3 / 고

- **결정**: 원본의 K–2/3–5/6–8/9–12를 한국 학년군으로 재편. 고교는 공통(통합과학1·2,
  과학탐구실험) / 선택 을 하나의 밴드로 묶되 내부에서 분기.
- **근거**: 교육과정 문서의 학년군 구획과 학습맵 데이터 구획(초등: 학년군, 중등:
  schoolLevel)이 이 경계와 일치.
- **초1–2 특례**: 초1–2에는 과학 교과가 없다(통합교과). science.md의 Clarify 절이
  소유한다 — 교사에게 안내한 뒤 조정을 제안한다.
- **재검토 조건**: 고교 공통/선택을 별도 밴드로 쪼갤 필요가 파일럿에서 확인되면.

### ADR-3. 학습맵 2종 병렬, 브리지는 추정 명시

- **결정**: 초등·중등 MCP를 모두 번들하되, 두 그래프가 교차 조회되지 않는다는 사실을
  호출 레퍼런스에 명시한다. 초→중 결손 추적은 키워드 병렬 검색 + "추정 연계" 표시.
- **근거**: 중등 교사의 초등 MCP 사용처는 사실상 선수학습 결손 진단 하나이며, 그 가치는
  실재한다. 그러나 근거 없는 코드 연결을 만들어내는 것은 학습맵 자체의 설계 원칙
  (공식 문서가 뒷받침하는 관계만 수록)과 모순된다.
- **기각한 대안**: (a) 초등 MCP 제외 — 결손 진단 가치 포기. (b) 수동 브리지 매핑 테이블
  동봉 — 데이터 프로젝트로 비대해지고 유지보수 주체가 불분명. 학습맵 저장소 쪽 기여로
  보내는 게 맞는 물건.
- **재검토 조건**: 병렬 검색의 소음이 크면 초등 조회를 명시 요청 시에만으로 조인다.

### ADR-4. 렌더링: docx 유지, HWPX는 2단계에 OWPML 직접 생성으로 추가

- **결정 (2026-08-01 개정)**: 1단계는 원본 docx/html 렌더러 무수정. HWPX는 2단계에서
  `render_lesson_hwpx.py`가 lesson.json → OWPML(section0.xml) **직접 생성**하는 방식으로
  추가하며 docx를 대체하지 않고 병행한다. 치수의 단일 소스는 원본 렌더러의 pt 명세
  (lesson_common.py의 학년 밴드 프로필·표 높이)다.
- **근거**: (a) 수업 자료는 docx로도 한국 학교에서 문제없다 — HWPX의 실수요는
  기안문·공문 맥락. (b) 스킬은 한 턴 결정론적 렌더를 보장해야 하므로 외부 도구 연결
  여부에 의존하지 않는 스크립트 내 생성이 안정적. (c) 렌더러 교체를 pedagogy
  검증과 분리해야 실패 지점을 격리할 수 있다.
- **기각한 대안**: (a) kordoc MCP 번들 + 모델이 generate_document 호출 — 렌더 일관성이
  모델 판단에 노출됨. (b) **kordoc CLI 마크다운 경유 어댑터 (원안)** — 재검토 조건이
  실측으로 발동되어 기각 (아래).
- **재검토 이력 (2026-08-01, 원안 기각)**: 파일럿 6종을 kordoc 경유로 시험 생성한 실측에서
  내용·인코딩은 보존됐으나 마크다운 단계에서 pt 명세가 전부 탈락했다 — 답란 높이 붕괴
  (108pt→45pt 등), 콜아웃 박스 소실, 상단 여백 4배. 보정은 section0.xml 직접 패치로만
  가능했다 (docs/superpowers/pilot-notes.md "HWPX 레이아웃 보정"). 결론: 마크다운을
  경유하지 않는다.
- **검증 경로**: 로컬 HWPX 툴체인(구조 검증 + 한컴 COM 실열림·쪽수 실측)을 QA로 쓴다.
  출처(Canine89/hwpxskill)가 라이선스 미표기임을 확인(2026-08-01)했으므로 저장소에
  벤더링하지 않는다 (.gitignore 처리, 배포물 아님). 어댑터 자체는 오리지널 코드로 작성한다.
- **미해결 (어댑터 구현 범위)**: 콜아웃 테두리·표 헤더 음영(borderFill), 큰 고정높이 표
  앞의 쪽 나눔 로직, number_line 블록의 OWPML 대응.
- **재개정 (2026-08-03, docx 산출 제거 — 소유자 결정)**: **교사 전달물을 HWPX 단일**로
  확정하고 docx 산출을 제거했다. `render_lesson_docx.py` 삭제, `render_all.sh`가 한
  명령으로 HTML과 HWPX를 모두 렌더한다 — **HWPX만 교사에게 제시하고, HTML은 수정 턴의
  재렌더용 내부 작업 파일이다**(SKILL.md가 교사 대면 메시지에서 `.html` 언급·링크를
  금지한다). 요청 시 옵션이던 HWPX를 기본 전달물로 격상한 것이 이 재개정의 핵심이다.
  근거: (a) 실사용 대상이 한국 학교라 docx 수요가 없었다. (b) python-docx 의존이
  사라져 렌더 의존성이 표준 라이브러리 0으로 줄었다. 대가: docx 렌더러 파일의 업스트림
  추적 종료 — 산출 스키마와 HTML 렌더러가 남은 업스트림 추적점이다. tests/check_lesson.py도
  hwpx 텍스트 추출 기반으로 재작성.

### ADR-5. (폐기 2026-08-01) 다과목 포팅 순서

- 과학 전용으로 범위를 확정하면서(§1.4) 다른 과목으로의 확장 계획과 함께 폐기했다.
  ADR 번호는 기존 문서·기록의 참조 보존을 위해 남긴다.

### ADR-6. SKILL.md는 diff, 과목 레퍼런스는 재작성

- **결정**: 오케스트레이터(SKILL.md)는 구조·단계·draft offer 메커니즘을 유지하는 최소
  diff(언어·도구명·가드레일 문구). 과목 레퍼런스는 원본 구조에 1:1 대응하되 내용은
  한국 체제로 재작성.
- **근거**: 원본 SKILL.md의 오케스트레이션(임계 실패 규정, 검색 상한, 병렬 배치 지시)은
  수개월의 프롬프트 엔지니어링 산물로 추정되며, 건드릴수록 회귀 위험. 반면 과목
  레퍼런스는 내용 자체가 미국산이라 diff로는 안 된다.

---

## 5. 불변 원칙 (모든 구현 파일이 지켜야 할 것)

1. **성취기준 원문은 verbatim.** 학습맵의 공식 원문을 수업안 콜아웃에 그대로 인용한다.
   바꿔 쓰지 않는다 — 이것이 이 프로젝트가 훈련 지식 기반 생성과 구별되는 지점이다.
2. **근거 없는 관계를 만들지 않는다.** 학습맵에 없는 선수관계·연계는 "추정" 표시 없이
   단정하지 않는다. 학습맵의 보수적 데이터 철학을 스킬 층에서 훼손하지 않는다.
3. **미연결 시 강등, 실패 아님.** 학습맵 미연결이면 면책 푸터와 함께 완전 동작한다.
4. **교과서 출판사 중립.** 검정 교과서의 활동·지문·삽화·문항을 재현하지 않는다. 교사가
   확언하지 않은 출판사명은 산출물·채팅 어디에도 쓰지 않는다. (원본 IM/OSE 가드레일의
   한국판 — 저작권과 공정성 양쪽 근거)
5. **산출 스키마 동결.** material-source JSON 스키마는 무수정 유지 — 스키마 호환이 곧
   원본 업스트림 추적 가능성이다. 렌더러 구성은 ADR-4 이력을 따른다(2026-08-03부터
   교사 전달물 HWPX + 내부 HTML, docx 렌더러 제거). "하나의 소스에서 전 문서 렌더 →
   드리프트 불가" 속성은 형식과 무관하게 유지한다.
6. **탐구 우선, 설명 후행.** 학생이 증거를 다루기 전에 교사가 결론을 내려주지 않는다 —
   전 학년군 공통의 pedagogy 불변 원칙 (원본에서 계승, 한국 체제와도 정합).

---

## 6. 데이터 소스와 알려진 공백

### 6.1 사용 가능

| 데이터 | 소스 | 비고 |
|---|---|---|
| 성취기준 원문 | 학습맵 2종 (초 620 + 중고 2,887) | NCIC 공개 PDF 추출·검증본 |
| 세부 학습 주제 | 동 (초 1,956 + 중고 4,333) | **관찰 증거·평가 문항 포함** — look-for와 정리 문항의 시드 |
| 선수관계 | 동 (초 1,894 + 중고 213) | 중고는 보수적 수록 (공식 근거만) |
| 중→고 전이 | 중등 학습맵 175건 | 공식 근거 기반 |

### 6.2 공백과 대응

| 공백 | 원본에는 있었나 | 대응 | 장기 |
|---|---|---|---|
| 오개념 데이터 | ○ (KG misconception 도구) | 훈련 지식 폴백. 단, 외부 오개념 MCP가 연결되면 자동 우선 (도구명 감지) | 오개념 벤치마크 프로젝트가 MCP로 공급자화되면 플러그인 확장 |
| 커리큘럼 자료 검색 | ○ (IM/OpenSciEd) | 단계 생략, 성취기준·주제만으로 독립 설계 | textbook_wiki 류의 지식 기반이 MCP화되면 재검토 |
| 초→중 브리지 | △ (단일 그래프라 문제 자체가 없었음) | ADR-3 | 학습맵 저장소 기여 대상 |
| 고교 특성화·특목 | — | 명시적 범위 제외 (§1.4) | — |

**공백 대응의 공통 패턴**: 이 플러그인은 데이터 공백을 자체 데이터로 메우지 않는다.
인터페이스(도구명 감지, 생략 규정)만 열어두고, 데이터는 별도 MCP 프로젝트가 공급한다.
플러그인은 얇게, 데이터는 그래프에.

---

## 7. 단계별 로드맵과 완료 기준 (Definition of Done)

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| **1. 과학 파일럿** | 골격 + .mcp.json + curriculum-kr-mcp.md + science.md + SKILL.md diff | 실 MCP 연결 상태에서 중학교 1회·초등 1회 수업 생성. 체크: 성취기준 원문 verbatim 인용 ○ / look-for에 관찰 증거 반영 ○ / 3범주 목표 진술 자연스러움 ○(ADR-1 검증) / 시간 배분 현실성 ○ |
| **2. HWPX** (완료 2026-08-01) | render_lesson_hwpx.py (OWPML 직접 생성, ADR-4 개정판) | 표 포함 수업안이 한글에서 깨짐 없이 열림, docx 병행 유지 — 11개 문서 전수: 구조 검증·한컴 COM 실열림 PASS, 고정 높이 행 docx 렌더러와 전 항목 일치 |
| **3. 공개** | README(attribution 포함), GitHub 공개(완료 2026-08-01), example_lesson.json 과학 소재 한국어 예시로 교체(완료 2026-08-01), differentiation 스킬 2차 포팅(완료 2026-08-01), 마켓플레이스 매니페스트(완료 2026-08-03), 설치본 스모크 검증(완료 2026-08-03), docx 산출 제거·HWPX 기본화(완료 2026-08-03, ADR-4 재개정) | 외부인이 README만으로 설치·1회 생성 성공 — 충족(2026-08-03, pilot/smoke-test) |

**각 단계는 공개 가능한 상태로 끝난다** — 3단계까지 미루지 않는다. 1단계 완료 시점에
"과학 전용 프리뷰"로 저장소를 공개하는 것이 기본값이다 (가시성 정체 패턴의 구조적 차단).

## 8. 라이선스·표기

- Apache-2.0 유지. 모든 파생 파일에 SPDX 헤더로 원저작(Anthropic, PBC / Learning
  Commons) + 기여자 표기, NOTICE 파일 유지.
- 학습맵 2종·kordoc은 번들이 아니라 의존(npx 런타임 해석)이므로 코드 동봉 없음 —
  README에 데이터 출처와 각 저장소 링크 명시.
- 성취기준 원문은 국가 교육과정 고시문 — 출처(교육부 고시)를 README에 명시.

## 9. 문서 지도

| 문서 | 역할 | 상태 |
|---|---|---|
| **DESIGN.md (이 문서)** | canonical 기준 — 왜 | v0.4 |
| .claude-plugin/marketplace.json | 저장소 루트 마켓플레이스 매니페스트 — `/plugin marketplace add`의 진입점. 플러그인 본체는 `source: "./plugin"` | 2026-08-03 추가. `claude plugin validate` 통과, 로컬 경로로 add→install→details end-to-end 확인(스킬 2·MCP 2 인식) |
| plugin/skills/ko12-lesson-planning/SKILL.md | 오케스트레이터 (원본 최소 diff) | 파일럿 검증본 |
| plugin/skills/ko12-lesson-planning/references/curriculum-kr-mcp.md | Step 2 호출 시퀀스 — 표준 조회 방법 | 파일럿 검증본 (실 MCP 대조 보정 완료) |
| plugin/skills/ko12-lesson-planning/references/science.md | 과학 pedagogy 레퍼런스 | 파일럿 검증본 |
| plugin/skills/ko12-lesson-differentiation/ | 차별화 스킬 (SKILL.md + science.md + curriculum-kr-mcp.md + scripts) | 2차 포팅 완료 (2026-08-01). R1–R8을 3범주 프레임으로 재작성, 실생성 4문서 검증 |
| plugin/skills/ko12-lesson-planning/scripts/render_lesson_hwpx.py | HWPX 어댑터 (OWPML 직접 생성) | 2단계 구현·전수 검증 완료. 타이포그래피 보정 1회(2026-08-01), 한글 세부 조판은 추가 작업 대기 |
| plugin/skills/ko12-lesson-planning/references/example_lesson.json | 산출 JSON 스키마 예시 | 과학·한국어판으로 교체 (2026-08-01). 파일럿 #1 기반 + 번호 과제·힌트 카드 문서로 스키마 폭 유지 |
| docs/superpowers/pilot-notes.md | 파일럿·구현 실측 기록 — 무엇을 확인했나 | 1–3단계 기록 (3단계: 마켓플레이스 배포·설치본 스모크·경로 강건화·docx 제거, 2026-08-03) |
| tests/check_lesson.py | DoD 자동 검증기 (결정론, 표준 라이브러리만) | hwpx 텍스트 추출 기반으로 재작성 (2026-08-03, ADR-4 재개정에 따라) |
| docs/evals-port-review.html | evals 한국판 보정 전수 대조 — 항목별 원본/보정 나란히, 변경 성격 3분류(A 측정 대상 부재 / B 우리 스펙 편 / C 계측기 수리) | `docs/evals_port_review.py`가 생성 (2026-08-03). 판정표는 그 스크립트의 `CLASSIFICATION`에 있다 |
| evals/ | 산출물 채점 루브릭 (LLM-judge용, tests/의 결정론 검증을 보완) | **한국판 보정 완료 (2026-08-03)** — 69항목. 폴더명 `ko12-*`로 정렬. 재설계 4(P-S1·P-S4 NGSS 3차원→3범주, M-CLARIFY-STATE→학년 확인 + 초1–2 특례) · 보정 22 · 나머지 원본 유지. 채점 기준 열의 미국 체제 잔재 0건 전수 확인. 각 항목 Notes에 원본 대비 개변 사유 기록 |
